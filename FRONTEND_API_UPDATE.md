# Обновление Backend API: Гайд для фронтенда

Ребята, привет! Мы выкатили пачку обновлений. Вот что нужно знать для интеграции.

---

## 1. Демо-голоса (ElevenLabs)
Теперь у каждого голоса есть прямая ссылка на аудио-пример (`mp3`).

- **Эндпоинт**: `GET /api/v1/audio/voices`
- **Что изменилось**: В объекте голоса появилось поле `sampleUrl`.
- **Пример ответа**:
```json
{
  "id": "Rachel",
  "name": "Rachel",
  "description": "Standard voice",
  "sampleUrl": "https://pub-f7e229b86c1940fabdcf50f072f1013a.r2.dev/voice_samples/f416f062-2e97-4b9c-8083-3aee86ef386e.mp3"
}
```
**Совет:** Используйте стандартный тег `<audio>` для воспроизведения примера рядом с выбором голоса.

---

## 2. Чат ИИ (OpenRouter & Gemini 3.1)
Мы переехали на OpenRouter и самую свежую модель.

- **Модель по умолчанию**: `gemini-2.5-flash` (внутри теперь мапится на `google/gemini-3.1-flash-lite-preview`).
- **Стоимость**: Учтите, что входные токены стали дешевле (50 руб за 1М), а выходные — 300 руб за 1М.
- **Интерфейс**: Никаких изменений в структуре запросов/ответов чата нет, всё прозрачно.

---

## 3. История транзакций (Balance History)
Добавлен эндпоинт для отображения истории списаний и пополнений.

- **Эндпоинт**: `GET /api/v1/payments/history`
- **Метод**: `GET`
- **Заголовок**: `Authorization: Bearer <token>`
- **Формат ответа (массив объектов)**:
```json
[
  {
    "id": 12,
    "amount": 0.054,
    "type": "spend", // 'spend' - списание, 'deposit' - пополнение
    "description": "Чат ИИ (google/gemini-3.1-flash-lite-preview): Привет...",
    "createdAt": "2026-03-05T09:30:00.000Z"
  }
]
```
**Важно:** В описании (`description`) теперь всегда содержится краткая инфа о том, за что списали деньги (модель, часть промпта).

---

## 4. Глобальные цены
Если вам нужно отображать стоимость услуг в интерфейсе, актуальные тарифы теперь такие:
- **Gemini 3.1 Flash Lite**: 50/300 руб за 1М токенов.
- **Suno V4**: 60 руб за генерацию.
- **ElevenLabs**: 6 руб за 1000 символов (Turbo).

Удачи в интеграции! Если есть вопросы — пишите Асоль (бэкенд). 🚀
